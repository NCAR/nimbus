
#include <stdio.h>

char buffer[2500];

main()
{
  int	i;
  int	r, g, b;

  r = 65025; g = 65025; b = 65025;
  for (i = 0; i < 30; ++i)
    {
    printf("{%d, %d, %d},\n",
		r - (i * (65025 / 30)), g - (i * (65025 / 30)), b);
    }


  r = 0; g = 0; b = 65025;
  for (i = 0; i < 30; ++i)
    {
    printf("{%d, %d, %d},\n",
		r, g + (i * (65025 / 30)), b - (i * (65025 / 30)));
    }
/*
  r = 0; g = 32512; b = 65025;
  for (i = 0; i < 15; ++i)
    {
    printf("{%d, %d, %d},\n",
		r, g + (i * (65025 / 30)), b - (i * (65025 / 15)));
    }
*/
  r = 0; g = 65025; b = 0;
  for (i = 0; i < 20; ++i)
    {
    printf("{%d, %d, %d},\n",
		r + (i * (65025 / 20)), g, b);
    }

  r = 65025; g = 65025; b = 0;
  for (i = 0; i < 40; ++i)
    {
    printf("{%d, %d, %d},\n",
		r, g - (i * (65025 / 40)), b);
    }


}
