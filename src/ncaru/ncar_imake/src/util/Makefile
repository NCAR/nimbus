# Makefile generated by imake - do not edit!
# $XConsortium: imake.c,v 1.87 94/04/17 20:10:28 rws Exp $

# -----------------------------------------------------------------------
# Makefile generated from "Imake.tmpl" and <Imakefile>
# $Id: Imake.tmpl,v 1.1.1.1 1999/05/14 20:57:07 morreale Exp $
#

.SUFFIXES: .i

# $Id: Imake.cf,v 1.1.1.1 1999/05/14 20:57:09 morreale Exp $

# -----------------------------------------------------------------------
# site-specific configuration parameters that need to come before
# the platform-specific parameters - edit site.def to change

# site:  $Id: site.def,v 1.1.1.1 1999/05/14 20:57:08 morreale Exp $

# -----------------------------------------------------------------------
# platform-specific configuration parameters - edit linux.cf to change

# platform:  $Id: linux.cf,v 1.4 1998/12/17 20:38:11 cruff Exp $

# operating system:  Linux_x86 2 2 9
# OSMajorNumber:	 2
# OSMinorNumber:	 2
# OSTeenyNumber:	 9

# -----------------------------------------------------------------------
# site-specific configuration parameters that go after
# the platform-specific parameters - edit site.def to change

# site:  $Id: site.def,v 1.1.1.1 1999/05/14 20:57:08 morreale Exp $

# $Id: Imake.rules,v 1.1.1.1 1999/05/14 20:57:09 morreale Exp $
# -----------------------------------------------------------------------
# Imake rules for building libraries, programs, scripts, and data files

          PATHSEP = /
            SHELL = /bin/sh

              TOP = ../..
      CURRENT_DIR = src/util

            IMAKE = $(IMAKESRC)/ncar_imake
           DEPEND = $(DEPENDSRC)/ncar_makedepend
        MKDIRHIER = $(SHELL) $(UTILSRC)/ncar_mkdirhier.sh
        CONFIGSRC = $(TOP)/cf
         IMAKESRC = $(BINSRC)/ncar_imake
        DEPENDSRC = $(BINSRC)/ncar_makedepend

          INCROOT = /tmp/morreale/include
        LINKLIBDIR = /tmp/morreale/lib
         SHLIBDIR = /tmp/morreale/lib
       LINTLIBDIR = $(LINKLIBDIR)/lint
          MANPATH = /tmp/morreale/man
    MANSOURCEPATH = $(MANPATH)/man
           MANDIR = $(MANSOURCEPATH)$(MANSUFFIX)
        LIBMANDIR = $(MANSOURCEPATH)$(LIBMANSUFFIX)
       FILEMANDIR = $(MANSOURCEPATH)$(FILEMANSUFFIX)

               AR = ar clq
  BOOTSTRAPCFLAGS =
               CC = cc
               AS = as

         COMPRESS = compress
              CPP = /lib/cpp $(STD_CPP_DEFINES)
    PREPROCESSCMD = cc -E $(STD_CPP_DEFINES)
          INSTALL = install
     INSTALLFLAGS = -c
               LD = ld
              LEX = flex -l
           LEXLIB = -lfl
             YACC = yacc
           CCYACC = yacc
             LINT = lint
      LINTLIBFLAG = -C
         LINTOPTS = -axz
               LN = ln -s
             MAKE = make
               MV = mv
               CP = cp

           RANLIB = ranlib
  RANLIBINSTFLAGS =

               RM = rm -f
        MANSUFFIX = 1
     LIBMANSUFFIX = 3
    FILEMANSUFFIX = 4
            TROFF = psroff
         MSMACROS = -ms
              TBL = tbl
              EQN = eqn

           PURIFY = purify
    PURIFYOPTIONS =

     STD_INCLUDES =
  STD_CPP_DEFINES = -traditional
      STD_DEFINES =
 EXTRA_LOAD_FLAGS =
  EXTRA_LDOPTIONS =
  EXTRA_LIBRARIES =
             TAGS = ctags
               M4 = m4

    PROTO_DEFINES =

     INSTPGMFLAGS =

     INSTBINFLAGS = -m 0755
     INSTUIDFLAGS = -m 4755
     INSTLIBFLAGS = -m 0644
     INSTINCFLAGS = -m 0644
     INSTMANFLAGS = -m 0644
     INSTDATFLAGS = -m 0644
    INSTKMEMFLAGS = -m 4755

      PROJECTROOT = /tmp/morreale

     TOP_INCLUDES = -I$(BUILDINCROOT)

      CDEBUGFLAGS = -O
        CCOPTIONS =
    CCMDLINEFLAGS =
       CSITEFLAGS =
      LDSITEFLAGS =
      CLOCALFLAGS =
     LDLOCALFLAGS =

      ALLINCLUDES = $(INCLUDES) $(EXTRA_INCLUDES) $(TOP_INCLUDES) $(STD_INCLUDES)

 PLATFORM_DEFINES = -DArchLinux -DOSMajorVersion=2 -DOSMinorVersion=2
       ALLDEFINES = $(ALLINCLUDES) $(STD_DEFINES) $(EXTRA_DEFINES) 			$(PROTO_DEFINES) $(THREADS_DEFINES) 			$(PLATFORM_DEFINES) $(DEFINES)

           CFLAGS = $(CCMDLINEFLAGS) $(CDEBUGFLAGS) $(CLOCALFLAGS) 			$(CSITEFLAGS) $(CCOPTIONS) 			$(THREADS_CFLAGS)  $(ALLDEFINES)

        LINTFLAGS = $(LINTOPTS) -DLINT $(ALLDEFINES) $(DEPEND_DEFINES)
         LDPRELIB = -L$(BUILDLIBDIR)
        LDPOSTLIB = -L$(LINKLIBDIR)
        LDOPTIONS = $(CDEBUGFLAGS) $(LDLOCALFLAGS) $(LDSITEFLAGS) 			$(CCOPTIONS)  $(EXTRA_LDOPTIONS) 			$(THREADS_LDFLAGS) $(LOCAL_LDFLAGS) $(LDPRELIB)

     CXXLDOPTIONS = $(CXXDEBUGFLAGS) $(CXXLDLOCALFLAGS) $(CXXLDSITEFLAGS) $(CXXOPTIONS) $(EXTRA_LDOPTIONS) $(THREADS_CXXLDFLAGS) $(LOCAL_LDFLAGS) $(LDPRELIB)

           LDLIBS = $(LDPOSTLIB) $(THREADS_LIBS) $(SYS_LIBRARIES) $(EXTRA_LIBRARIES)

           CCLINK = $(CC)

          CXXLINK = $(CXX)

     LDSTRIPFLAGS = -x
   LDCOMBINEFLAGS = -X -r
      DEPENDFLAGS =
    DEPEND_DEFINES =

        MACROFILE = linux.cf
           RM_CMD = $(RM)

IMAKE_DEFINES	=

IRULESRC        = $(CONFIGSRC)
IMAKE_CMD 	= $(IMAKE) 		-I$(PRIVCONFIGDIR) -I$(PUBCONFIGDIR) 		-DPubConfigDir=$(PUBCONFIGDIR) $(IMAKE_DEFINES)

# -----------------------------------------------------------------------
# X Window System Build Parameters and Rules
# $Id: Project.tmpl,v 1.1.1.1 1999/05/14 20:57:08 morreale Exp $

CONFIGROOTDIR	= $(LIBDIR)/cf
PRIVCONFIGDIR	= $(TOP)/cf

#
# This is the path to the "public" ncar_imake config files. It will
# be set in "private" Makefiles when booted via ncar_imake.
#
PUBCONFIGDIR	= $(LIBDIR)/cf

BUILDROOT	= $(TOP)

# -----------------------------------------------------------------------
# X Window System make variables; these need to be coordinated with rules

PROJECTNAME	= ncar_imake

BINDIR 		= /tmp/morreale/bin
LINKLIBDIR 	= /tmp/morreale/lib
LIBDIR 		= $(LINKLIBDIR)/$(PROJECTNAME)
INCROOT		= /tmp/morreale/include
INCDIR 		= $(INCROOT)/$(PROJECTNAME)

BUILDINCROOT 	= $(BUILDROOT)/include
BUILDINCDIR 	= $(BUILDINCROOT)/$(PROJECTNAME)
BUILDINCTOP 	= ../..
BUILDLIBDIR 	= $(BUILDROOT)/lib
BUILDLIBTOP 	= ..

LINKLIBSRC 	= $(TOP)/src/lib
BINSRC 		= $(TOP)/src/bin
UTILSRC		= $(TOP)/src/util

DEPLIBS = $(LOCAL_LIBRARIES)

        CONFIGDIR = $(LIBDIR)/cf

# -----------------------------------------------------------------------
# start of Imakefile

# BASED ON:
# $Id: Imakefile,v 1.1.1.1 1999/05/14 20:57:11 morreale Exp $

PROJECTNAME	= ncar_imake
PUBCONFIGDIR 	= $(LIBDIR)/cf
PROGRAMS	= ncar_mkdirhier ncar_imboot ncar_imdent ncar_bsdinst 		ncar_install

INSTALLFLAGS	= $(INSTPGMFLAGS)

all:: $(PROGRAMS)
install:: install.ncar_mkdirhier install.ncar_imboot install.ncar_imdent 	install.ncar_bsdinst install.ncar_install

ncar_mkdirhier: ncar_mkdirhier.sh
	$(CP) ncar_mkdirhier.sh ncar_mkdirhier
	chmod a+x ncar_mkdirhier

install.ncar_mkdirhier:: ncar_mkdirhier
	@if [ -d $(DESTDIR)$(BINDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(BINDIR)); fi
	@case '${MFLAGS}' in *[i]*) set +e;; esac; \
	for i in ncar_mkdirhier; do \
	(set -x; $(INSTALL) $(INSTALLFLAGS) $(INSTBINFLAGS) $$i $(DESTDIR)$(BINDIR)); \
	done

install.man:: ncar_mkdirhier.man
	@if [ -d $(DESTDIR)$(MANDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(MANDIR)); fi
	$(INSTALL) $(INSTALLFLAGS) $(INSTMANFLAGS) ncar_mkdirhier.man $(DESTDIR)$(MANDIR)/ncar_mkdirhier.1

ncar_imboot::  ncar_imboot.cpp $(ICONFIGFILES)
	$(RM) $@
	$(CPP) -DPUBCONFIGDIR=$(PUBCONFIGDIR) <ncar_imboot.cpp | sed -e '/^#  *[0-9][0-9]*  *.*$$/d' -e '/^XCOMM$$/s//#/' -e '/^XCOMM[^a-zA-Z0-9_]/s/^XCOMM/#/' >$@
	chmod a+x $@

clean::
	$(RM) ncar_imboot

install.ncar_imboot:: ncar_imboot
	@if [ -d $(DESTDIR)$(BINDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(BINDIR)); fi
	@case '${MFLAGS}' in *[i]*) set +e;; esac; \
	for i in ncar_imboot; do \
	(set -x; $(INSTALL) $(INSTALLFLAGS) $(INSTBINFLAGS) $$i $(DESTDIR)$(BINDIR)); \
	done

ncar_imdent::  ncar_imdent.pl
	$(RM) $@
	$(CPP)  <ncar_imdent.pl | sed -e '/^#  *[0-9][0-9]*  *.*$$/d' -e '/^XCOMM$$/s//#/' -e '/^XCOMM[^a-zA-Z0-9_]/s/^XCOMM/#/' >$@
	chmod a+x $@

clean::
	$(RM) ncar_imdent

install.ncar_imdent:: ncar_imdent
	@if [ -d $(DESTDIR)$(BINDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(BINDIR)); fi
	@case '${MFLAGS}' in *[i]*) set +e;; esac; \
	for i in ncar_imdent; do \
	(set -x; $(INSTALL) $(INSTALLFLAGS) $(INSTBINFLAGS) $$i $(DESTDIR)$(BINDIR)); \
	done

ncar_bsdinst: ncar_bsdinst.sh
	$(CP) ncar_bsdinst.sh ncar_bsdinst
	chmod a+x ncar_bsdinst

install.ncar_bsdinst:: ncar_bsdinst
	@if [ -d $(DESTDIR)$(BINDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(BINDIR)); fi
	@case '${MFLAGS}' in *[i]*) set +e;; esac; \
	for i in ncar_bsdinst; do \
	(set -x; $(INSTALL) $(INSTALLFLAGS) $(INSTBINFLAGS) $$i $(DESTDIR)$(BINDIR)); \
	done

ncar_install: ncar_install.sh
	$(CP) ncar_install.sh ncar_install
	chmod a+x ncar_install

install.ncar_install:: ncar_install
	@if [ -d $(DESTDIR)$(BINDIR) ]; then set +x; \
	else (set -x; $(MKDIRHIER) $(DESTDIR)$(BINDIR)); fi
	@case '${MFLAGS}' in *[i]*) set +e;; esac; \
	for i in ncar_install; do \
	(set -x; $(INSTALL) $(INSTALLFLAGS) $(INSTBINFLAGS) $$i $(DESTDIR)$(BINDIR)); \
	done

clean::
	$(RM) $(PROGRAMS)

depend::

# -----------------------------------------------------------------------
# common rules for all Makefiles - do not edit

.c.i:
	$(RM) $@
	$(CC) -E $(CFLAGS) $(_NOOP_) $*.c > $@

emptyrule::

clean::
	$(RM_CMD) *.CKP *.ln *.BAK *.bak *.o core errs ,* *~ *.a .emacs_* tags TAGS make.log MakeOut PureFilesToClean  "#"*

Makefile:: $(IMAKE)

$(IMAKE):
	-@(cd $(IMAKESRC); if [ -f Makefile ]; then \
	echo "checking $@ in $(IMAKESRC) first..."; $(MAKE) all; else \
	echo "bootstrapping $@ from Makefile.ini in $(IMAKESRC) first..."; \
	$(MAKE) -f Makefile.ini BOOTSTRAPCFLAGS="$(BOOTSTRAPCFLAGS)"; fi; \
	echo "okay, continuing in $(CURRENT_DIR)")

Makefile::
	-@if [ -f Makefile ]; then set -x; \
	$(RM) Makefile.bak; $(MV) Makefile Makefile.bak; \
	else exit 0; fi
	$(IMAKE_CMD) -DTOPDIR=$(TOP) -DCURDIR=$(CURRENT_DIR)

tags::
	$(TAGS) -w *.[ch]
	$(TAGS) -xw *.[ch] > TAGS

# -----------------------------------------------------------------------
# empty rules for directories that do not have SUBDIRS - do not edit

install::
	@echo "install in $(CURRENT_DIR) done"

install.man::
	@echo "install.man in $(CURRENT_DIR) done"

Makefiles::

includes::

depend::

# -----------------------------------------------------------------------
# dependencies generated by ncar_makedepend

