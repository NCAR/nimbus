The architecture specific directories (linux, mac and windows) are where 
distribution products are built for that specific architecture. Architecture
specific run scripts and resources are also located here.

The bitrock directory is where BitRock installers are created, and re-packaged
(e.g. dmg for OSX) if necessary.
 