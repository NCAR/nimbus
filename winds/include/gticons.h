/* gticons.h -- create icons for button help images

   The icons used here are created via the Icon Editor program using Screen
   Regular font, size 14 (Open Windows, v2) or Lucida-Regular-Medium-Default
   font, Open Windows V3....

   Each icon is read into a short int, then written onto a pixrect, which
   is positioned on a canvas.

*/

/* here's all the help icons  */

static unsigned short pick_text_item[] = {   
#include <icons/pick_text_item>
};

static unsigned short pick_panel_item[] = {   
#include <icons/pick_panel_item>
};

static unsigned short select_dismiss[] = {   
#include <icons/select_dismiss>
};

static unsigned short click_to_zoom[] = {
#include <icons/click_to_zoom>
};

static unsigned short click_to_unzoom[] = {
#include <icons/click_to_unzoom>
};

static unsigned short chg_full_menu[] = {
#include <icons/chg_full_menu>
};

static unsigned short add_full_menu[] = {
#include <icons/add_full_menu>
};

static unsigned short extendor[] = {
#include <icons/extendor>
};

static unsigned short solo_toggle_fix[] = {
#include <icons/solo_toggle_fix>
};

static unsigned short solo_toggle_mode[] = {
#include <icons/solo_toggle_mode>
};

static unsigned short solo_defrng[] = {
#include <icons/solo_defrng>
};

static unsigned short global_toggle_fix[] = {
#include <icons/global_toggle_fix>
};

static unsigned short global_toggle_mode[] = {
#include <icons/global_toggle_mode>
};

static unsigned short global_defrng[] = {
#include <icons/global_defrng>
};

static unsigned short delete_hilited[] = {
#include <icons/delete_hilited>
};

static unsigned short lmark_delete[] = {
#include <icons/lmark_delete>
};

static unsigned short blank[] = {
#include <icons/blank>
};
