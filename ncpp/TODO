- Initial window will have 3 XY plots (default 60 seconds of averaging)
  showing number (CFSSP), surface, and volume.

- Be able to overlap different time segments on same plot, or plain
  one plot every N seconds.. in multiple XY plots, one 3d plot, or a
  movie.

- At bottom have statistics for diameter (average, mode, median, and
  effective) or stats window if too much stuff.

- Curve fits (log normal, exponential, weibull).
  - Display co-effiecients.
  - Be able to show curve on plot.

- Convert 2d data to 1d data.
